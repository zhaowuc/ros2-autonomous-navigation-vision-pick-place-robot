# Install as ~/.config/environment.d/60-roscar-local-dds.conf on the vehicle.
# All ROS 2 nodes run on car1, so Wi-Fi roaming must not rebuild the local DDS
# graph used by the GUI, Nav2, safety chain, and serial base interface.
ROS_LOCALHOST_ONLY=1
ROS_DOMAIN_ID=0
